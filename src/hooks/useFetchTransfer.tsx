// import { useState } from "react"


export const useFetchTransfer = async() => {

  // const [transfer, setTransfer] = useState([]);

  // const getTransfers = async => {
  //   const newTransfers = await getTransfers(localization);
  // }

  return {
    transfer: [],

    isLoading: true,
  }
}
